app = 'pushup-bot'
primary_region = 'ams'

[build]

[processes]
  app = 'python main.py'

[deploy]
  # Один екземпляр — два запущених боти надсилали б дублікати
  strategy = 'immediate'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '256mb'
